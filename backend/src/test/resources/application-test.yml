spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/insurance_claims_test
      database: insurance_claims_test
  
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: test-client-id
            client-secret: test-client-secret
            scope:
              - email
              - profile

server:
  port: 0

logging:
  level:
    com.insurance.claims: DEBUG
    org.springframework.security: DEBUG
